version: "3.3"  
services:
  mongodb:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - ~/apps/mongo:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=wpereira
      - MONGO_INITDB_ROOT_PASSWORD=wpereira